import styles from "./library-side-bar.module.css";

type Props = {
  
};

const LibrarySideBar = ({}: Props) => {
  return (
    <div className={styles.container}></div>
  );
};

export default LibrarySideBar;