import styles from "./library-data.module.css";

type Props = {
  
};

const LibraryData = ({}: Props) => {
  return (
    <div className={styles.container}></div>
  );
};

export default LibraryData;